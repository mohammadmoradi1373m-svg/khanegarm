# خانه‌گرم / owli.ir — V9 Final Regression Audit

## هدف
بررسی کامل نسخه V8 بعد از مشاهده خرابی «ثبت نظر» و پیدا کردن regressionهای ناشی از مهاجرت Hash Router به Clean URL.

## ایرادهای پیدا شده و اصلاح‌شده

### 1) ثبت نظر — Critical regression
- `submitReview()` هنوز `parseHash()` را صدا می‌زد.
- در نسخه جدید دیگر `parseHash()` وجود نداشت و router با `parseLocation()` کار می‌کرد.
- نتیجه: کلیک روی «ثبت نظر» قبل از RPC با `ReferenceError` متوقف می‌شد.
- اصلاح: `parseHash()` → `parseLocation()`.
- تست runtime با mock RPC انجام شد و `p_product_id = p1` درست به `submit_review` رسید.

### 2) جستجوی صفحه اصلی — Broken function reference
- هنگام بازنویسی router، تابع `runHomeSearch()` از نسخه قبلی حذف شده بود ولی دو کنترل صفحه اصلی هنوز آن را صدا می‌زدند.
- اصلاح: تابع اصلی جستجو دوباره در router جدید قرار گرفت.
- تست static reference: PASS.
- تست runtime router: PASS.

### 3) URL محصول — query تکراری
- `go('/product?id=...')` در router جدید می‌توانست `/product/p1?id=p1` بسازد.
- اصلاح: وقتی `id` داخل `/product/:id` قرار گرفته، دیگر در query تکرار نمی‌شود.
- خروجی صحیح: `/product/p1`.

### 4) شمارنده سفارش‌های تحویل‌شده — DB/Frontend permission mismatch
- صفحه اصلی `get_delivered_orders_count()` را بدون session صدا می‌زند.
- migration وضعیت سفارش در فایل نسخه قبلی، `EXECUTE` را از `anon` هم revoke کرده بود؛ در نتیجه این بخش از frontend عملاً silently fail می‌کرد.
- در Production اصلاح شد: `anon` و `authenticated` برای این RPC مجاز شدند؛ دسترسی `PUBLIC` همچنان باز نیست.
- Production همچنین constraint وضعیت سفارش `-1..3` را ندارد/داشتن آن بررسی شد و در این نسخه برقرار شد.

## بررسی regression نسبت به baseline
مقایسه نام توابع نسخه baseline با V9 انجام شد:
- تنها تابع حذف‌شده عمدی: `parseHash()`.
- هیچ تابع حذف‌شده دیگری که هنوز در کد صدا زده شود باقی نمانده.
- `runHomeSearch()` که accidental حذف شده بود، برگردانده شد.

## تست‌های انجام‌شده
- JavaScript syntax: PASS — 4 executable script blocks
- PHP syntax: PASS
- HTML IDs: 163 unique / 163 — duplicate: 0
- Canonical tag: 1
- Robots meta: 1
- Inline handler unresolved function check: PASS
- Literal DOM reference check: PASS (dynamic IDs excluded where generated by render functions)
- `eval()` / `new Function()`: none
- service_role / Turnstile secret in frontend: none
- Router runtime checks: PASS
- Review submission runtime mock: PASS
- PHP public-host SPA routing: PASS
- PHP API TRACE guard: PASS (`METHOD_NOT_ALLOWED`)
- Sitemap XML parse: PASS

## Production DB checks
- `orders_status_valid` constraint: present; allowed statuses `-1,0,1,2,3`
- Admin order status RPCs: `anon` execute = false
- `submit_review`: `anon` execute = true
- `get_delivered_orders_count`: `anon` execute = true after V9 fix

## محدودیت تست
تست واقعی Browser → `https://owli.ir` → `https://api.owli.ir` → Supabase از محیط ChatGPT قابل اجرای کامل نیست.
پس تست نهایی روی هاست واقعی بعد از deploy همچنان با برچسب زیر لازم است:

**نیازمند تست روی هاست واقعی است**

موارد مهم برای تست واقعی:
1. ثبت نظر با Turnstile واقعی
2. جستجوی صفحه اصلی
3. باز شدن مستقیم `/product/<id>`
4. مهاجرت لینک قدیمی `#/product?id=<id>`
5. نمایش شمارنده سفارش‌های تحویل‌شده
6. ثبت سفارش و پیگیری سفارش
7. آپلود محصول/دسته/هیرو توسط ادمین
