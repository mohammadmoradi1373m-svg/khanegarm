# OWLI / خانه‌گرم — وضعیت نهایی بررسی 2026-09-20

## انجام‌شده
- چهار Hero نهایی `hero-home-1.webp` تا `hero-home-4.webp` با ابعاد 1920×1080 در GitHub موجود هستند.
- Patch اولیه V24 در GitHub آپلود شده است.
- منبع فعلی `index.html` بررسی شد.
- مشخص شد V24 هنوز روی `main` اعمال نشده و دلیل نمایش دو کارت، نبودن CSS V24 در `index.html` است.
- نسخه اصلاح‌شده V24.1 آماده شد.
- Supabase: دسترسی `PUBLIC/anon` به RPCهای `admin_*` قبلاً revoke شده و migration `revoke_public_admin_rpc_execute` ثبت شده است.
- `customers.phone` در حال حاضر Primary Key است؛ بنابراین uniqueness شماره مشتری برقرار است.
- `orders` دارای محدودیت وضعیت و سازگاری پرداخت است.
- RPC `create_order_idempotent` در دیتابیس موجود است.

## اصلاحات V24.1
- One-card-per-view برای Carousel محصولات تا عرض 720px
- Swipe + scroll-snap
- حذف فلش‌های Carousel در موبایل
- بهبود خوانایی Hero
- کاهش جزئی تراکم Typography موبایل
- کاهش ارتفاع Footer موبایل
- کوچک‌تر شدن دکمه واتساپ و جلوگیری از پوشاندن کارت محصول

## مواردی که از داخل این محیط قابل اعمال نبود
اتصال GitHub فعلی برای عملیات Write با خطای HTTP 403 `Resource not accessible by integration` مواجه شد؛ بنابراین V24.1 را نمی‌توانستم مستقیماً روی `main` commit کنم.

## مواردی که هنوز نیازمند محیط واقعی هستند
- تست واقعی `owli.ir` روی مرورگر موبایل/دسکتاپ
- تست E2E ثبت سفارش
- تست واقعی پرداخت بانکی
- تست حساب، بازیابی رمز، پنل ادمین و تغییر وضعیت سفارش
- بررسی رفتار واقعی واتساپ/پیام‌رسان در هاست
- تأیید نهایی پس از Deploy

## Supabase — هشدارهای باقی‌مانده
Security Advisor هنوز برخی هشدارهای SECURITY DEFINER برای RPCهای عمومی/احراز هویت و هشدار Leaked Password Protection دارد. این‌ها را بدون بررسی وابستگی‌های UI و Auth نباید کورکورانه revoke کرد، چون بخشی از RPCها عمداً برای کاربر عمومی یا کاربر واردشده استفاده می‌شوند.

Performance Advisor نیز چندین هشدار مربوط به ارزیابی مجدد `auth.*` در RLS و چند index کم‌استفاده گزارش می‌کند. این‌ها بهینه‌سازی مرحله بعد هستند، نه خطای عملکردی اثبات‌شده.

## وضعیت
نسخه موبایل از نظر UI تقریباً آماده است؛ نقطه اصلی باقی‌مانده برای UI، اعمال V24.1 و سپس تست واقعی روی هاست است.
